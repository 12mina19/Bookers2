<!--<h1>Users#show</h1>-->
<!--ユーザー新規投稿が成功時、Welcome! You have signed up successfully.を表示-->
<%= notice %>

<!--ユーザのプロフィール更新が成功時、You have updated user successfully.を表示-->
<!--投稿の更新が成功時、You have updated book successfully.を表示-->
<%= flash[:complete] %>

<!--テンプレート：User info / New Book-->
<main>
  <p id="notice"></p>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">
        <%= render 'books/list', user: @user %>
      </div>

<!--右側のBooks作成-->
      <!--offset-md-1：左側に1カラムの余白を空ける-->
      <div class="col-md-8 offset-md-1">
        <h2>Books</h2>
        <!--table-hover：テーブルの各行にマウスがホバリング効果（灰色の背景）-->
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th></th>
              <th>Title</th>
              <th>Opinion</th>
              <!--colspan:横方向にセルを結合させる-->
              <th colspan="3"></th>
            </tr>
          </thead>
          
          <tbody>
            <% @books.each do |book| %>
            <tr>
              <td>
                <%= image_tag book.user.get_profile_image(80,80) %>
              </td>
              <td>
                <%= link_to book.title, book_path(book.id) %> 
              </td>
              <td>
                <%= book.body %>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>

    </div>
  </div>
</main>
