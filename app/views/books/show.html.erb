<!--<h2>Books#show</h2>-->
<!--新規投稿・投稿更新が成功した時、メッセージ表示：You have created book successfully／You have updated book successfully-->
<%= flash[:complete] %>

<!--テンプレート：User info / New Book-->
<main>
  <p id="notice"></p>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">
        <%= render 'books/list', user: @user %>
      </div>


<!--Book detail画面-->
  <div class="col-md-8 offset-md-1">
    <h2>Book detail</h2>
    <table class="table">
      <tbody>
        <tr>
          <td>
          <%= link_to user_path(@book.user.id) do %>
            <%= image_tag @book.user.get_profile_image(80,80) %><br><%= @book.user.name %>
          <% end %>
          </td>
          <td>
          <%= link_to @book.title, book_path(@book.id) %>
          </td>
          <td>
          <%= @book.body %>
          </td>
          
          <!--編集・削除ボタンを投稿した本人だけに表示させる-->
          <% if @book.user == current_user %>
            <td>
            <%= link_to "Edit", edit_book_path(@book.id), class: "btn btn-sm btn-success" %>
            </td>
            <td>
            <%= link_to "Destroy", book_path(@book.id), method: :delete, class: "btn btn-sm btn-danger", "data-confirm" => "本当に消しますか？" %>
            </td>
          <% end %>
        </tr>
      </tbody>
    </table>
  </div>


    </div>
  </div>
</main>
